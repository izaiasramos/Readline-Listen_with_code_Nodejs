# Readline-Listen_with_code_Nodejs
Usando Readline do Node para escutar e executar código no Terminal da IDE




# 🧮 Calculadora de IMC (Índice de Massa Corporal) em Node.js

Este é um projeto simples em Node.js que utiliza o módulo nativo `readline` para interagir com o usuário no terminal, solicitando o peso e a altura para calcular o IMC.

## 🌟 O que é o `readline`?

O `readline` é um **módulo nativo** do Node.js (ou seja, ele já vem com o Node, não precisa instalar) que permite **ler dados de uma _stream_ de leitura** (como o teclado do usuário) **linha por linha**. É a maneira padrão de criar programas interativos de linha de comando.

## 🚀 Como Executar o Programa

Siga estes passos simples para rodar o código no seu terminal:

### 1. Pré-requisito

Você precisa ter o **Node.js** instalado na sua máquina.

### 2. Salvar o Código

Crie um arquivo chamado **`index.js`** e cole o código nele.

### 3. Abrir o Terminal

Abra o seu terminal (ou Prompt de Comando/PowerShell) na **mesma pasta** onde você salvou o arquivo `index.js`.

### 4. Executar o Comando

Digite o seguinte comando e pressione Enter:

```bash
node index.js
